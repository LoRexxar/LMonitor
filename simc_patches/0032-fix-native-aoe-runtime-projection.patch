diff --git a/engine/action/action.hpp b/engine/action/action.hpp
index 9b663568ae..9aedad0cca 100644
--- a/engine/action/action.hpp
+++ b/engine/action/action.hpp
@@ -693,6 +693,11 @@ public:
         || (weapon && weapon_multiplier > 0);
   }
 
+  /** True when a class module installs this action on a recipient actor and
+   *  requires a separate source actor before its damage can be evaluated. */
+  virtual bool external_recipient_action() const
+  { return false; }
+
   bool does_periodic_damage() const;
   bool does_direct_damage() const;
 
diff --git a/engine/class_modules/monk/sc_monk.cpp b/engine/class_modules/monk/sc_monk.cpp
index db8d36caa4..c8dc351300 100644
--- a/engine/class_modules/monk/sc_monk.cpp
+++ b/engine/class_modules/monk/sc_monk.cpp
@@ -1601,6 +1601,7 @@ struct spinning_crane_kick_t : public monk_melee_attack_t
     {
       dual = background = true;
       aoe               = -1;
+      reduced_aoe_targets = player->talent.windwalker.jade_ignition->effectN( 3 ).base_value();
     }
   };
 
diff --git a/engine/class_modules/paladin/sc_paladin_retribution.cpp b/engine/class_modules/paladin/sc_paladin_retribution.cpp
index e9454be269..63e85a240a 100644
--- a/engine/class_modules/paladin/sc_paladin_retribution.cpp
+++ b/engine/class_modules/paladin/sc_paladin_retribution.cpp
@@ -446,6 +446,7 @@ struct divine_storm_second_sunrise_tempest_t : public holy_power_consumer_t<pala
     background = true;
 
     aoe = -1;
+    reduced_aoe_targets = p->talents.divine_storm->effectN( 2 ).base_value();
     base_multiplier *= p->talents.tempest_of_the_lightbringer->effectN( 1 ).percent();
     clears_judgment          = false;
     is_divine_storm          = true;
@@ -473,6 +474,7 @@ struct divine_storm_tempest_t : public holy_power_consumer_t<paladin_melee_attac
     background = true;
 
     aoe = -1;
+    reduced_aoe_targets = p->talents.divine_storm->effectN( 2 ).base_value();
     base_multiplier *= p->talents.tempest_of_the_lightbringer->effectN( 1 ).percent();
     clears_judgment          = false;
     is_divine_storm          = true;
@@ -502,6 +504,7 @@ struct divine_storm_second_sunrise_t : public holy_power_consumer_t<paladin_mele
     background = true;
 
     aoe = -1;
+    reduced_aoe_targets = p->talents.divine_storm->effectN( 2 ).base_value();
     base_multiplier *= multiplier;
     clears_judgment                   = false;
     base_costs[ RESOURCE_HOLY_POWER ] = 0;
diff --git a/engine/class_modules/sc_demon_hunter.cpp b/engine/class_modules/sc_demon_hunter.cpp
index 1b58bb568e..ceb3f7e01a 100644
--- a/engine/class_modules/sc_demon_hunter.cpp
+++ b/engine/class_modules/sc_demon_hunter.cpp
@@ -4503,6 +4503,7 @@ struct collective_anguish_t : public demon_hunter_spell_t
     {
       dual = true;
       aoe  = -1;
+      reduced_aoe_targets = p->find_spell( 198013 )->effectN( 5 ).base_value();
     }
   };
 
@@ -7925,6 +7926,7 @@ struct inner_demon_t : public demon_hunter_spell_t
     : demon_hunter_spell_t( name, p, p->spec.inner_demon_damage )
   {
     aoe = -1;
+    reduced_aoe_targets = p->talent.havoc.inner_demon->effectN( 2 ).base_value();
   }
 };
 
diff --git a/engine/class_modules/sc_evoker.cpp b/engine/class_modules/sc_evoker.cpp
index 4f8a909ba2..0fd48e1755 100644
--- a/engine/class_modules/sc_evoker.cpp
+++ b/engine/class_modules/sc_evoker.cpp
@@ -2468,6 +2468,9 @@ public:
   {
   }
 
+  bool external_recipient_action() const override
+  { return true; }
+
   action_state_t* new_state() override
   {
     return new state_t( this, ab::target );
@@ -5388,6 +5391,7 @@ struct azure_sweep_t : public azure_strike_base_t
       mid1_es_cdr( -p->sets->set( EVOKER_DEVASTATION, MID1, B4 )->effectN( 2 ).time_value() )
   {
     aoe = -1;
+    reduced_aoe_targets = data().effectN( 1 ).base_value();
   }
 
   bool action_ready() override
diff --git a/engine/class_modules/sc_warrior.cpp b/engine/class_modules/sc_warrior.cpp
index 67973b1eb4..95f65d0475 100644
--- a/engine/class_modules/sc_warrior.cpp
+++ b/engine/class_modules/sc_warrior.cpp
@@ -3926,6 +3926,7 @@ struct thunder_blast_t : public warrior_attack_t
   {
     parse_options( options_str );
     aoe       = -1;
+    reduced_aoe_targets = data().effectN( 5 ).base_value();
     may_dodge = may_parry = may_block = false;
 
     // Shared cooldown with thunder clap
@@ -4062,6 +4063,7 @@ struct thunder_clap_t : public warrior_attack_t
   {
     parse_options( options_str );
     aoe       = -1;
+    reduced_aoe_targets = data().effectN( 5 ).base_value();
     may_dodge = may_parry = may_block = false;
 
     cooldown = p->cooldown.thunder_clap;
@@ -5913,6 +5915,9 @@ struct revenge_t : public warrior_attack_t
     {
       parse_options( options_str );
       aoe           = -1;
+      // SpellDescriptionVariables for Revenge defines $cap=${5}; the spell has
+      // no dedicated effect slot carrying this threshold in the exact-build DBC.
+      reduced_aoe_targets = 5;
 
       if ( p->talents.mountain_thane.lightning_strikes->ok() )
       {
diff --git a/engine/sc_main.cpp b/engine/sc_main.cpp
index 91e2bba87a..63665527d4 100644
--- a/engine/sc_main.cpp
+++ b/engine/sc_main.cpp
@@ -612,6 +612,16 @@ void write_skill_damage_dbc_component( std::ostream& out,
 bool skill_damage_amount_changed( const skill_damage_amount_t& a, const skill_damage_amount_t& b )
 {
   const auto changed = []( double x, double y ) { return std::abs( x - y ) > std::max( 1.0, std::abs( x ) ) * 1.0e-8; };
+  const auto target_hit_changed = [ &changed ]( const auto& x, const auto& y ) {
+    if ( x.size() != y.size() )
+      return true;
+    auto xi = x.begin();
+    auto yi = y.begin();
+    for ( ; xi != x.end(); ++xi, ++yi )
+      if ( xi->first != yi->first || changed( xi->second, yi->second ) )
+        return true;
+    return false;
+  };
   const auto layers_changed = [&changed]( const skill_damage_runtime_layers_t& x,
                                           const skill_damage_runtime_layers_t& y ) {
     return changed( x.action_multiplier, y.action_multiplier ) ||
@@ -626,6 +636,8 @@ bool skill_damage_amount_changed( const skill_damage_amount_t& a, const skill_da
   return changed( a.direct_amount.expected, b.direct_amount.expected ) ||
          changed( a.tick_amount.expected, b.tick_amount.expected ) ||
          changed( a.tick_amount.damage_equivalent_count, b.tick_amount.damage_equivalent_count ) ||
+         target_hit_changed( a.direct_amount.target_hit, b.direct_amount.target_hit ) ||
+         target_hit_changed( a.tick_amount.target_hit, b.tick_amount.target_hit ) ||
          ( a.direct && b.direct && a.direct_amount.can_crit != b.direct_amount.can_crit ) ||
          ( a.periodic && b.periodic && a.tick_amount.can_crit != b.tick_amount.can_crit ) ||
          ( a.direct && b.direct &&
@@ -701,6 +713,17 @@ bool skill_damage_runtime_layers_valid( const skill_damage_runtime_layers_t& lay
          positive( layers.pet_multiplier ) && positive( layers.target_pet_multiplier );
 }
 
+// Runtime amount hooks may contain gameplay RNG (for example, proc-based
+// damage multipliers). Skill-damage probes are analytical snapshots, so each
+// independently evaluated layer/hit must start from the same deterministic
+// stream instead of inheriting call-order state from earlier scenarios.
+constexpr uint64_t skill_damage_probe_seed = 0x4c4d4f4e49544f52ULL;
+
+void skill_damage_reset_probe_rng( action_t& action )
+{
+  action.sim->rng().seed( skill_damage_probe_seed );
+}
+
 double skill_damage_target_hit( action_t& action, player_t* target, unsigned n_targets,
                                 unsigned chain_target, bool periodic )
 {
@@ -712,9 +735,11 @@ double skill_damage_target_hit( action_t& action, player_t* target, unsigned n_t
   hit_state->n_targets = n_targets;
   hit_state->chain_target = chain_target;
   hit_state->result = RESULT_HIT;
+  skill_damage_reset_probe_rng( action );
   action.snapshot_state( hit_state, amount_type );
   hit_state->attack_power = 100.0;
   hit_state->spell_power = 100.0;
+  skill_damage_reset_probe_rng( action );
   const double hit = periodic ? action.calculate_tick_amount( hit_state, 1.0 )
                               : action.calculate_direct_amount( hit_state );
   action_state_t::release( hit_state );
@@ -732,6 +757,7 @@ void skill_damage_populate_target_scenarios( action_t& action, skill_damage_amou
     throw std::runtime_error( "target scenario action target unavailable" );
 
   const int saved_desired_targets = action.sim->desired_targets;
+  const auto saved_non_sleeping_targets = action.sim->target_non_sleeping_list.data();
   try
   {
     for ( int scenario_count : scenario_counts )
@@ -739,6 +765,8 @@ void skill_damage_populate_target_scenarios( action_t& action, skill_damage_amou
       // Keep the full target count visible to class/action formulas while using
       // action_state_t's native n_targets/chain_target AoE calculation path.
       action.sim->desired_targets = scenario_count;
+      action.sim->target_non_sleeping_list.assign_without_callbacks(
+          as<size_t>( scenario_count ), action.target );
       const int configured_targets = action.n_targets();
       const int hit_targets = configured_targets < 0
                             ? scenario_count
@@ -765,9 +793,11 @@ void skill_damage_populate_target_scenarios( action_t& action, skill_damage_amou
   catch ( ... )
   {
     action.sim->desired_targets = saved_desired_targets;
+    action.sim->target_non_sleeping_list.assign_without_callbacks( saved_non_sleeping_targets );
     throw;
   }
   action.sim->desired_targets = saved_desired_targets;
+  action.sim->target_non_sleeping_list.assign_without_callbacks( saved_non_sleeping_targets );
 }
 
 skill_damage_amount_t skill_damage_runtime_amount( action_t& action )
@@ -789,19 +819,24 @@ skill_damage_amount_t skill_damage_runtime_amount( action_t& action )
     if ( action.does_direct_damage() )
     {
       state->result = RESULT_HIT;
+      skill_damage_reset_probe_rng( action );
       action.snapshot_state( state, result_amount_type::DMG_DIRECT );
+      skill_damage_reset_probe_rng( action );
       amount.direct_amount.runtime_layers = skill_damage_runtime_layers( state, false );
       amount.direct_amount.base_multiplier = action.action_multiplier();
       amount.direct_amount.base_component_multiplier = action.action_da_multiplier();
       amount.direct_amount.can_crit = action.may_crit;
       state->attack_power = 100.0;
       state->spell_power = 100.0;
+      skill_damage_reset_probe_rng( action );
       amount.direct_amount.hit = action.calculate_direct_amount( state );
+      skill_damage_reset_probe_rng( action );
       amount.direct_amount.crit_chance_uncapped = action.may_crit ? state->composite_crit_chance() : 0.0;
       amount.direct_amount.crit_chance = std::clamp( amount.direct_amount.crit_chance_uncapped, 0.0, 1.0 );
       if ( action.may_crit )
       {
         state->result = RESULT_CRIT;
+        skill_damage_reset_probe_rng( action );
         action.calculate_direct_amount( state );
         amount.direct_amount.crit = action.calculate_crit_damage_bonus( state );
         amount.direct_amount.crit_multiplier = 1.0 + state->result_crit_bonus;
@@ -827,7 +862,9 @@ skill_damage_amount_t skill_damage_runtime_amount( action_t& action )
     {
       state->initialize(); state->target = action.target;
       state->n_targets = 1; state->chain_target = 0; state->result = RESULT_HIT;
+      skill_damage_reset_probe_rng( action );
       action.snapshot_state( state, result_amount_type::DMG_OVER_TIME );
+      skill_damage_reset_probe_rng( action );
       amount.tick_amount.runtime_layers = skill_damage_runtime_layers( state, true );
       amount.tick_amount.base_multiplier = action.action_multiplier();
       amount.tick_amount.base_component_multiplier = action.action_ta_multiplier();
@@ -837,6 +874,10 @@ skill_damage_amount_t skill_damage_runtime_amount( action_t& action )
       if ( tick_duration <= 0_ms || tick_period <= 0_ms )
       {
         amount.unresolved_reason = "periodic_damage_count_unavailable";
+        // A missing periodic equivalent count must not discard an already
+        // resolved direct component or its native multi-target projection.
+        if ( amount.direct )
+          skill_damage_populate_target_scenarios( action, amount );
         action_state_t::release( state );
         return amount;
       }
@@ -850,12 +891,15 @@ skill_damage_amount_t skill_damage_runtime_amount( action_t& action )
       amount.tick_amount.damage_equivalent_count = full_ticks + partial_tick + immediate_tick;
       state->attack_power = 100.0;
       state->spell_power = 100.0;
+      skill_damage_reset_probe_rng( action );
       amount.tick_amount.hit = action.calculate_tick_amount( state, 1.0 );
+      skill_damage_reset_probe_rng( action );
       amount.tick_amount.crit_chance_uncapped = action.tick_may_crit ? state->composite_crit_chance() : 0.0;
       amount.tick_amount.crit_chance = std::clamp( amount.tick_amount.crit_chance_uncapped, 0.0, 1.0 );
       if ( action.tick_may_crit )
       {
         state->result = RESULT_CRIT;
+        skill_damage_reset_probe_rng( action );
         amount.tick_amount.crit = action.calculate_tick_amount( state, 1.0 );
         amount.tick_amount.crit_multiplier = 1.0 + state->result_crit_bonus;
       }
@@ -1197,23 +1241,6 @@ void prepare_skill_damage_dbc_actions( player_t* player )
   }
 }
 
-bool skill_damage_external_recipient_action( const action_t& action )
-{
-  // The Evoker module creates these actions on every player whenever any Evoker
-  // is present. Their source Evoker is assigned only when an external-support
-  // callback fires, so they are recipient-side team effects rather than the
-  // actor's own skills or selected-talent damage.
-  static const std::set<std::string_view> external_recipient_actions = {
-    "infernos_blessing",
-    "blistering_scales_damage",
-    "fate_mirror_damage",
-    "fate_mirror_heal",
-    "breath_of_eons_damage",
-    "bombardments",
-  };
-  return external_recipient_actions.find( action.name_str ) != external_recipient_actions.end();
-}
-
 bool skill_damage_residual_trigger_dependent( action_t& action )
 {
   action_state_t* state = action.get_state();
@@ -1228,11 +1255,26 @@ bool skill_damage_player_skill( const player_t& player, const action_t* reportin
   if ( !reporting_root || !reporting_root->data().ok() ) return false;
   const auto candidate_it = skill_damage_dbc_candidates.find( &player );
   if ( candidate_it == skill_damage_dbc_candidates.end() ) return false;
-  return std::any_of(
+  if ( std::any_of(
       candidate_it->second.begin(), candidate_it->second.end(),
       [ reporting_root ]( const skill_damage_dbc_candidate_t& candidate ) {
         return std::find( candidate.actions.begin(), candidate.actions.end(), reporting_root ) !=
                candidate.actions.end();
+      } ) )
+    return true;
+
+  // Class modules also create background/triggered actions which are not
+  // directly addressable through action_names_from_spell_id(). Spell class
+  // family is the native DBC ownership fact: it includes those class actions,
+  // while excluding racial/profession/item spells (family 0) and recipient-side
+  // support actions created for a different class.
+  const unsigned reporting_family = reporting_root->data().class_family();
+  if ( !reporting_family ) return false;
+  return std::any_of(
+      candidate_it->second.begin(), candidate_it->second.end(),
+      [ &player, reporting_family ]( const skill_damage_dbc_candidate_t& candidate ) {
+        const auto* spell = player.find_spell( candidate.spell_id );
+        return spell && spell->ok() && spell->class_family() == reporting_family;
       } );
 }
 
@@ -1361,7 +1403,7 @@ void export_skill_damage( sim_t& sim )
     bool first_action = true;
     for ( action_t* action : player->action_list )
     {
-      if ( !action || skill_damage_external_recipient_action( *action ) ||
+      if ( !action || action->external_recipient_action() ||
            ( !action->does_direct_damage() && !action->does_periodic_damage() ) ) continue;
       const unsigned spell_id = apl_metadata_action_spell_id( *action );
 
diff --git a/engine/util/vector_with_callback.hpp b/engine/util/vector_with_callback.hpp
index b4c1aa0791..056114d483 100644
--- a/engine/util/vector_with_callback.hpp
+++ b/engine/util/vector_with_callback.hpp
@@ -73,6 +73,12 @@ public:
   void clear_without_callbacks()
   { _data.clear(); }
 
+  void assign_without_callbacks( size_t count, const T& value )
+  { _data.assign( count, value ); }
+
+  void assign_without_callbacks( const std::vector<T>& values )
+  { _data = values; }
+
 private:
   void trigger_callbacks( const T& v ) const
   {
